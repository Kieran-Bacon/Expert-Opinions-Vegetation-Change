CREATE TABLE expertModels (identifier TEXT PRIMARY KEY,
                           username TEXT,
                           qid INTEGER,
                           precision REAL,
                           accuracy REAL,
                           R2 REAL,
                           L1 REAL)